# 換行統一規則
#
# 為什麼要有這個檔：Windows 與 Mac 對「換行」的存法不同（Windows 每行結尾多一個
# 看不見的 \r）。沒有這個檔的話，同一份檔案在兩台機器之間來回，git 會整份判定
# 為「已修改」，但內容其實一個字都沒差——真正的改動會被這些假異動淹沒。
#
# 怎麼分辨假異動：git status 說有改、但 git diff 看不到任何差異，就是這種。
#
# text=auto 讓 git 自己判斷哪些是文字檔（看內容有沒有二進位位元組），
# 也不必逐一列出副檔名，連沒有副檔名的檔（.gitignore）都能涵蓋。

* text=auto eol=lf

# pre-commit 是金鑰掃描的 hook，沒有副檔名，必須是 LF，
# 否則 shebang 後面多一個 \r，Mac 上每一次 commit 都會失敗。
.githooks/*  text eol=lf

# ⚠️ 例外：這個檔在 git 裡目前是「換行混用」的（1339 行 CRLF + 379 行 LF），
# 是長期 Mac／Windows 交替編輯的結果。若套用上面的規則，下次 commit 會把整份
# 重寫成 1700 多行的假差異——2026-08-04 就踩過一次，當時是靠 Python 位元組層級
# 改寫才把 diff 壓回 44 增／16 刪。-text 表示「完全不要碰它的換行」，維持現狀。
# 之後若真要整理，應該單獨處理並在同一個 commit 裡說明。
index.html  -text
